# Copy this file to .env (same folder) and update the value below.
# Include the API Gateway stage path (e.g., /v1) but do not add a trailing slash afterward.
VITE_API_BASE_URL=https://your-api-id.execute-api.us-east-1.amazonaws.com/v1
